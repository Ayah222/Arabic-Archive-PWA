# دليل الاختبار الشامل — الميزات الجديدة
# أرشيف ذكي | يوليو 2026

---

## أولاً: كيف تفتح التطبيق؟

افتح المتصفح وانتقل إلى:
```
http://localhost:80/smart-archive/
```
أو من نافذة Replit → اختر أيقونة المعاينة واختر "أرشيف ذكي - Smart Archive PWA"

---

## ثانياً: قائمة كل الميزات الجديدة وأماكنها وطريقة اختبارها

---

### 🆕 برومبت 1 — الترقيم التلقائي وحالة التوزيع للخطابات
**أين تجده:** المشاريع ← أي مشروع ← تبويب "الخطابات"

**طريقة الاختبار:**
1. افتح مشروع "برج الأعمال المركزي"
2. انتقل لتبويب **الخطابات**
3. ستلاحظ على كل خطاب:
   - رقم مرجعي تلقائي مثل **LTR-2025-001**
   - شارة حالة التوزيع: (لم يُرسل / تم الإرسال / تم الاستلام)
4. اضغط **+ إضافة خطاب**
5. اكمل البيانات، وستجد:
   - **حقل حالة التوزيع** (اختار "تم الإرسال")
   - **الجهات المستلمة** (اختار: مالك + استشاري)
   - ستلاحظ الرسالة: "سيتم توليد رقم مرجعي تلقائي عند الحفظ"
6. اضغط حفظ ← ستجد الخطاب الجديد برقم مرجعي تلقائي جديد

**ما الذي يتحقق؟** الترقيم التلقائي يعمل + حقول الجهات المستلمة وحالة التوزيع موجودة

---

### 🆕 برومبت 2 — التحكم بالإصدارات (Revision Control) للمستندات
**أين تجده:** المشاريع ← أي مشروع ← تبويب "الملفات الفنية" (اسم جديد!)

**طريقة الاختبار:**
1. افتح مشروع "برج الأعمال المركزي"
2. انتقل لتبويب **الملفات الفنية** (كان اسمه "المستندات" قبل)
3. ستلاحظ على كل مستند:
   - رقم الإصدار: **Rev 0** أو **Rev 1**
   - شارة حالة الاعتماد: (قيد المراجعة / معتمد / مرفوض / معتمد مع ملاحظات)
4. المستند "المخططات المعمارية" يجب أن يظهر **Rev 1** و**معتمد** (بالأخضر)
5. مستند "مخططات التنسيق" في مشروع الواحة يجب أن يظهر **Rev 0** و**قيد المراجعة** (بالأصفر)

**اختبار API (من المتصفح أو curl):**
```
GET /api/sa/projects/proj-001/documents
```
ستجد في كل مستند: `revisions`, `currentRevision`, `approvalStatus`

---

### 🆕 برومبت 3 — التقارير الدورية
**أين تجده:** القائمة الجانبية ← **التقارير** (أيقونة الرسم البياني)

**طريقة الاختبار:**
1. اضغط "التقارير" من الـ Sidebar
2. ستجد تقريراً منفصلاً لكل مشروع يحتوي:
   - **ملخص عام**: إجمالي المستندات، الخطابات، العقود، الاجتماعات
   - **حالة المستندات**: قيد المراجعة / معتمد / مرفوض / متأخر
   - **المستندات المتأخرة**: (إذا وجدت)
   - **تغييرات الإصدارات** في الفترة
   - **خطابات بانتظار التأكيد**
   - **عقود قاربت الانتهاء**
3. اضغط زر **"أسبوعي"** أو **"شهري"** لتغيير الفترة
4. اضغط **"تصدير كـ PDF"** ← سيفتح نافذة طباعة جاهزة للطباعة/الحفظ كـ PDF

**الـ API:**
```
GET /api/sa/reports?period=weekly
GET /api/sa/reports/proj-001?period=monthly
```

---

### 🆕 برومبت 4 — التذكيرات والمتابعة التلقائية
**أين تجده:** القائمة الجانبية ← **الإشعارات**

**كيف يعمل؟** (تلقائي كل ساعة)
- أي مستند قيد المراجعة أكثر من 5 أيام → يظهر تحذير في الإشعارات
- أي خطاب صادر لم يُؤكد استلامه بعد 7 أيام → يظهر إشعار "متابعة مطلوبة"
- عقد ينتهي خلال 30 يوم → إشعار تذكيري

**طريقة الاختبار:**
1. افتح **الإشعارات** ← ستجد بعض الإشعارات التلقائية الموجودة في البيانات التجريبية
2. الجرس في الأعلى يعرض عدد الإشعارات غير المقروءة
3. لاختبار الإشعار الفوري: أضف مستنداً جديداً (سيكون حالته "قيد المراجعة") ← بعد 5 أيام سيظهر إشعار تلقائياً

---

### 🆕 برومبت 5 — جهات الاتصال لكل مشروع
**أين تجده:** المشاريع ← أي مشروع ← تبويب **"جهات الاتصال"** (آخر تبويب جديد)

**طريقة الاختبار:**
1. افتح مشروع "برج الأعمال المركزي"
2. انتقل لتبويب **جهات الاتصال** (التبويب الأخير)
3. ستجد 3 جهات جاهزة: المالك، الاستشاري، المقاول
4. اضغط **+ إضافة جهة**
5. اختر الدور: (مالك / استشاري / مقاول / مكتب فني / أخرى)
6. أدخل الاسم والهاتف والبريد والملاحظات ← احفظ

**الـ API:**
```
GET /api/sa/projects/proj-001/contacts
POST /api/sa/projects/proj-001/contacts
DELETE /api/sa/projects/proj-001/contacts/:cid
```

---

### 🆕 برومبت 6 — البحث الذكي بالصوت (NLP)
**أين تجده:** زر الميكروفون في أسفل الشاشة (الزر الـ FAB)

**طريقة الاختبار:**
1. اضغط زر الميكروفون (أسفل يمين الشاشة)
2. جرب هذه الأوامر الطبيعية:
   - **"آخر خطاب صادر لمشروع البرج"** ← سيجد آخر خطاب صادر
   - **"ابحث عن مجمع الواحة"** ← بحث في كل البيانات
   - **"المستندات المعلقة قيد المراجعة"** ← يعرض المستندات المعلقة
   - **"ذكرني بموعد الاجتماع"** ← ينشئ إشعار تذكيري

**الـ API:**
```
POST /api/sa/voice
Body: { "text": "آخر خطاب صادر لمشروع البرج" }
```

---

### 🆕 برومبت 7 — الصلاحيات وسجل التدقيق
**أين تجده:** القائمة الجانبية ← **المستخدمون** (أيقونة الأشخاص)

**طريقة الاختبار (الدخول كمدير أولاً):**
1. افتح `/smart-archive/login` أو اضغط "تسجيل الدخول" (إن وُجد)
2. ادخل: **admin / admin123** (مدير - صلاحية كاملة)
3. انتقل لصفحة **المستخدمون**
4. ستجد تبويبين:
   - **المستخدمون**: قائمة بجميع الحسابات مع صلاحياتهم
   - **سجل التدقيق**: كل عملية (إضافة/تعديل/حذف) مع من قام بها ومتى
5. جرب تغيير صلاحية مستخدم: اضغط "تغيير" ← اختر دوراً جديداً ← احفظ

**الحسابات التجريبية:**
| الحساب | كلمة المرور | الصلاحية |
|--------|------------|---------|
| admin | admin123 | مدير (كامل) |
| entry | entry123 | موظف إدخال |
| viewer | viewer123 | عرض فقط |

---

### 🆕 برومبت 8 — تسجيل حسابات ذاتي
**أين تجده:** `/smart-archive/login` ← تبويب "حساب جديد"

**طريقة الاختبار:**
1. اذهب إلى `/smart-archive/login`
2. اضغط على تبويب **"حساب جديد"**
3. أدخل: الاسم الكامل + اسم المستخدم + كلمة المرور (6 أحرف على الأقل)
4. اضغط "إنشاء الحساب"
5. سيتم تسجيل الدخول تلقائياً وتوجيهك للصفحة الرئيسية
6. الحساب الجديد يحصل على صلاحية "موظف إدخال" تلقائياً

---

### 🆕 برومبت 9 — حدود الباقات (الهيكل جاهز)
**ملاحظة:** الحدود مُعرَّفة في `PROJECT_STRUCTURE.md` ومُخطَّطة للتنفيذ الكامل مع قاعدة البيانات.
- النظام يدعم مفهوم `workspaceId` في كل جدول
- الحدود: free (3 مشاريع، 100MB) / pro (50 مشروع، 5GB)

---

### 🆕 برومبت 10 — بنية Multi-Tenant
**أين تجده:** ملف `PROJECT_STRUCTURE.md` في جذر المشروع

**طريقة الاختبار:**
1. افتح الملف `PROJECT_STRUCTURE.md`
2. ستجد قسم "بنية متعددة المستأجرين" يشرح:
   - كيف تضيف `workspaceId` لكل جدول
   - SQL لإنشاء جداول `workspaces` و`workspace_members`
   - Middleware للفلترة التلقائية
   - جدول مقارنة مزودي الاستضافة

---

### 🆕 برومبت 11 — الأسئلة الشائعة وجولة الإعداد
**أين تجده:**
- **الأسئلة الشائعة:** القائمة الجانبية ← **"الأسئلة الشائعة"** (أيقونة علامة الاستفهام)
- **جولة الإعداد:** تظهر تلقائياً عند أول دخول للمستخدم الجديد

**طريقة اختبار الأسئلة الشائعة:**
1. اضغط "الأسئلة الشائعة" من الـ Sidebar
2. ستجد 12 سؤالاً شائعاً مصنفة بأيقونات
3. جرب البحث في صندوق البحث أعلى الصفحة
4. اضغط على أي سؤال لعرض الإجابة التفصيلية

**طريقة اختبار جولة الإعداد:**
1. افتح وضع التصفح الخاص (Incognito) في المتصفح
2. افتح `/smart-archive/`
3. بعد 1.2 ثانية ستظهر نافذة الجولة التعريفية تلقائياً (4 خطوات)
4. يمكنك الضغط "التالي" للتنقل أو "تخطي" لإغلاقها
5. بعد إغلاقها لن تظهر مرة أخرى للجهاز نفسه

---

### 🆕 برومبت 12 — الأتمتة التلقائية (Scheduled Jobs)
**أين يعمل:** في الخادم (API Server) تلقائياً كل ساعة

**ما يفعله تلقائياً:**
- ✅ يفحص المستندات "قيد المراجعة" أكثر من 5 أيام → ينشئ إشعار تحذيري
- ✅ يفحص الخطابات الصادرة غير المؤكدة بعد 7 أيام → ينشئ إشعار متابعة
- ✅ يفحص العقود التي تنتهي خلال 30 يوم → ينشئ إشعار تذكيري
- ✅ يفحص التذكيرات المالية خلال 3 أيام → ينشئ إشعار مالي

**طريقة التحقق:**
```
GET /api/sa/notifications
```
ستجد إشعارات تلقائية موجودة بالفعل من تشغيل الـ Scheduler

---

### 🆕 برومبت 13 — الاستقلالية عن Replit
**أين تجده:** ملف `PROJECT_STRUCTURE.md`

**ما الذي يضمن الاستقلالية:**
- ✅ Node.js / Express معيار صناعي
- ✅ PostgreSQL + Drizzle ORM معيار صناعي
- ✅ React + Vite يعمل على أي CDN
- ✅ متغيرات البيئة عبر `.env`
- ✅ لا يوجد أي API حصري بـ Replit في الكود

**خطوات النقل مشروحة في** `PROJECT_STRUCTURE.md` — قسم "نقل المشروع لاستضافة خارجية"

---

## ثالثاً: مصفوفة الاختبار السريع (Checklist)

```
قبل العرض للعميل، تأكد من:

[ ] لوحة التحكم تعرض الإحصاءات بشكل صحيح
[ ] صفحة المشاريع تعرض المشاريع الثلاثة
[ ] تفاصيل أي مشروع تفتح وتعرض التبويبات الستة:
    [ ] العقود
    [ ] المقاولون
    [ ] الملفات الفنية (وليس "المستندات")
    [ ] الاجتماعات
    [ ] الخطابات (مع رقم مرجعي تلقائي وشارة الحالة)
    [ ] جهات الاتصال (تبويب جديد)
[ ] صفحة التقارير تعرض تقرير لكل مشروع
[ ] زر "تصدير كـ PDF" يفتح نافذة الطباعة
[ ] صفحة الأسئلة الشائعة تعرض الأسئلة وتدعم البحث
[ ] صفحة تسجيل الدخول تعمل بالحسابات التجريبية
[ ] صفحة المستخدمون تعرض قائمة الحسابات وسجل التدقيق
[ ] الإشعارات تحتوي على تنبيهات تلقائية
[ ] زر الميكروفون يستجيب للأوامر الصوتية
[ ] الجولة التعريفية تظهر في وضع التصفح الخاص
```

---

## رابعاً: اختبار الـ API مباشرة (اختياري للتقنيين)

يمكنك تجربة أي API من المتصفح أو curl:

```bash
# الإشعارات
curl http://localhost:80/api/sa/notifications

# تقرير مشروع
curl "http://localhost:80/api/sa/reports/proj-001?period=weekly"

# جهات الاتصال
curl http://localhost:80/api/sa/projects/proj-001/contacts

# تسجيل الدخول
curl -X POST http://localhost:80/api/sa/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# سجل التدقيق
curl http://localhost:80/api/sa/audit

# البحث الصوتي
curl -X POST http://localhost:80/api/sa/voice \
  -H "Content-Type: application/json" \
  -d '{"text":"آخر خطاب صادر لمشروع البرج"}'

# المستخدمون
curl http://localhost:80/api/sa/users
```

---

## خامساً: ملاحظات مهمة

1. **البيانات في الذاكرة**: أي بيانات تضيفها ستختفي عند إعادة تشغيل الخادم. هذا طبيعي في مرحلة التطوير.

2. **الحسابات التجريبية**: لا تشارك كلمات المرور هذه مع أي عميل. لمرحلة الإنتاج، يجب ربط النظام بقاعدة بيانات حقيقية.

3. **التصدير PDF**: يعتمد على وظيفة الطباعة في المتصفح. ينصح بـ Chrome/Edge للحصول على أفضل نتيجة.

4. **الجولة التعريفية**: تظهر مرة واحدة فقط لكل متصفح. لرؤيتها مجدداً: افتح الـ DevTools ← Application ← localStorage ← احذف مفتاح `sa_onboarding_done`.

5. **المهام المجدولة**: تعمل كل ساعة. لرؤية نتيجتها الفورية: أعد تشغيل الخادم (تعمل فور الإقلاع).

---

*وثيقة مُنشأة: يوليو 2026 | جاهزة للمراجعة قبل العرض للعميل*
